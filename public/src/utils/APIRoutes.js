
// Define the host URL where your API is hosted
const host = "http://localhost:5000";

// Create a constant for the registration route by concatenating the host and the route path
export const registerRoute = `${host}/api/auth/register`;

// Create a constant for the login route by concatenating the host and the route path
export const loginRoute = `${host}/api/auth/login`;

